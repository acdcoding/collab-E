name: Enforce Branching Workflow

on:
  pull_request:
    branches:
      - main
    paths-ignore:
      - '.github/workflows/enforce-branching.yml'

jobs:
  enforce-development-to-main:
    runs-on: ubuntu-latest
    steps:
      - name: Check source branch
        run: |
          if [[ "${{ github.event.pull_request.title }}" == *"[skip-branch-check]"* ]]; then
            echo "Bypassing branch check due to '[skip-branch-check]' in PR title."
            exit 0
          fi

          if [ "${{ github.head_ref }}" != "development" ]; then
            echo "Error: Pull requests to 'main' are only allowed from 'development'."
            echo "Please target your pull request to the 'development' branch instead."
            exit 1
          fi
